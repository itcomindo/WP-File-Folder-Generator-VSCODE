{
  "name": "masmon-wordpress-theme-folders-and-files-generator",
  "displayName": "Masmon WordPress Theme Folders and Files Generator",
  "description": "Generate folders and files structure for a WordPress theme",
  "version": "0.0.2",
  "publisher": "budiharyono",
  "engines": {
    "vscode": "^1.50.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.generateWpTheme"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.generateWpTheme",
        "title": "Masmon WordPress Theme Folders and Files Generator"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "echo Skipping compile",
    "compile": "echo Nothing to compile"
  },
  "devDependencies": {
    "typescript": "^4.0.3",
    "@types/node": "^14.14.6",
    "@types/vscode": "^1.50.0"
  }
}
